# 学習履歴・復習システム 実装状況

## 実装済み

- `NGSL_1_2` / `NAWL_1_2` と出典順位から不変の `wordId` を生成する。
- 出題方向を含む `LearningItem ID` を生成する。
- 回答事実と集約状態を別テーブルに保存するRoom Database version 1。
- 回答イベントと集約状態を同じトランザクションで保存するRepository。
- 自力正解、補助あり正解、想起失敗を分離したデータモデル。
- 最終正式成功日時と間隔計算基準日時を分離した状態モデル。
- 1日、3日、7日、習得後14日、30日のVersion 1スケジューラ。
- 不正解時の段階低下、不正解日時を基準とする次回日時計算。
- 未ログインやoverdueだけでは状態を変更しない判定。
- 直近3回中2回失敗の苦手判定。
- 対象LearningItem集合を母数とする習得率計算。
- 時刻を外部から渡せる構造。
- Room schema JSONの保存。
- 通常回答、スキップ、時間切れから回答履歴への接続。
- デバッグ回答と未回答での画面離脱を履歴から除外。
- 異なる4 LearningItemまたは10分経過による短期復習解禁。
- 復習コース、苦手単語コース、習得率・初回自力正答率表示。
- 復習・苦手コースは1セッション1巡で終了し、直後の再出題を防ぐ。

## 採用した回答ルール

- 1回の出題では最初の回答だけを習得状態の遷移に使用する。
- 最初に失敗した後の正解は補助あり完了として元イベントへ追記する。
- 同一問題内で段階を戻すのは1回だけとする。
- スキップと時間切れは想起失敗、未回答での離脱は成績対象外とする。
- 品詞と通常補足はヒント扱いせず、先頭文字・答え表示後は補助ありとする。
- 短期復習成功はキュー解除だけに使い、正式段階は必要時間前には進めない。
- 初回自力正答率と補助あり完了を分離する。

## 残作業

1. 接続した実機またはエミュレーターでRoom計装テストを実行する。
2. Room version更新時のmigrationテストを追加する。
3. 習得率を主・マップ解放などのゲーム進行へ接続する。
4. 必要に応じて学習履歴の削除・書き出しUIを追加する。
