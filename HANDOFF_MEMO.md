# プロジェクト概要

- Android向けの「英単語学習 × フィッシングゲーム」。日本語の問題を見て英単語を入力し、アルファベットリールの回転数で魚を巻き上げる。
- Kotlin、Jetpack Compose、Material 3、ViewModel、Room、SharedPreferencesを使用。minSdk 26、targetSdk 36。
- 現在は、タイトルから魚とのバトル、捕獲・逃走、図鑑記録まで通せるプレイアブルプロトタイプ。完成ビジュアル、釣り操作、コンテンツ量は未完成。
- 最初の完成目標は「最初の渓流マップを完成品質で遊べて、解放した釣り堀で単語学習・復習ができる状態」。釣り堀自体は未実装。
- 画風は3Dドット絵。舞台の方向性は里川ではなく山中の渓流。

# 現在実装済みの機能

## 完成しているもの

- `words.json`から6,700語（NGSL 2,809語＋NAWL 957語＋公開語彙資料を照合した追加2,934語）を読み込み、`List<Word>`へ変換・検証する。
- ゲームレベル1〜7、学年、英検相当推定級で出題範囲を選択できる。
- 初回のゲーム開始時は、能力判定を行わず、ユーザー自身が目標の英検相当推定級を選ぶ。選択はSharedPreferencesへ保存し、2回目以降は目標選択を飛ばしてマップへ進む。問題設定では目標級で通常学習を開始でき、目標はいつでも変更できる。学年とゲームレベルは内部データとして維持するが通常UIには表示しない。
- 目標級の通常問題は、下位級が存在する範囲で20問単位に目標級15問（75%）、直下の級3問（15%）、さらに下の基礎確認2問（10%）を混ぜる。基礎確認枠は下位の各級を近い順に巡回し、苦手・復習対象、未出、その他の順で優先する。目標より上の級は出さない。4級目標は4級と5級だけ、5級目標は5級だけにする。比率は実プレイで調整する初期値。
- 問題は短い代表意味と品詞を表示し、辞書全文も開ける。各コース内の「意味＋品詞」の重複はデータ検査で禁止している。
- 単語をシャッフルし、全単語を一周するまで重複させず、周回境界でも同じ単語を連続させない。
- ASCII英字だけを小文字で入力へ採用し、入力中に文字別・合計リール回転数を計算する。
- 通常バトルの入力欄にA〜Zリール本体を表示し、文字別・合計回転数を既存計算から描画する。各文字はA始点・前文字始点の規則どおり時計回りにアニメーションする。
- 出題形式として単語入力、文章穴埋め入力、文章4択、意味4択、ミックスを選べる。文章未登録語は単語入力へ戻り、復習・苦手では文章4択を文章入力、意味4択を単語入力へ切り替える。
- 正解送信後は各リールが同時に回り続け、左端から1個ずつ停止する。停止した文字が正解文字なら緑のフラッシュ・弾み・チェックを表示し、最後の停止演出が完了するまで次問へ進まない。巻き上げバーには入力・選択中の回答による到達予測を別色で重ねる。
- 4択は未選択時からリール領域を確保し、1回目のタップで強調選択、同じ選択肢の2回目のタップで回答を確定する。
- 意味4択は既存6,700語を対象に、同一コース内から同じ品詞を優先して誤答候補を作る。選択肢は問題開始時に固定する。
- プロジェクト独自作成の文章問題1500問を`question_content.json`から読み込み、英文・和文・解説・正解・誤答3語・権利情報を検証する。
- 文章問題1500問の文頭・句読点・空白・答え漏れ・冠詞・表示重複を全件校正済み。95問の誤答候補140語を、文中で必要な品詞と難易度を基準に修正した。
- 正解、不正解、スキップ、回答時間、スコア、連続正解、糸のテンション、捕獲・逃走を処理する。
- 異なる誤答3回で先頭文字、5回で答えを表示する。同じ誤答の再入力は誤答数とテンションへ重複加算しない。
- 魚ごとにHP・距離・サイズを範囲内で生成し、正解時の回転数でHPと距離を減らす。HPは画面に表示しない。
- 捕獲数、最大サイズ、初回・最新捕獲日時、初回・最新のマップとポイントをSharedPreferencesへ保存する。
- Roomへ単語ごとの回答履歴と学習状態を保存し、通常・復習・苦手単語コース、習得数・習得率を扱える。
- デバッグビルドに正解、不正解、魚切替、即捕獲の操作と、文章問題だけで開始する導線がある。デバッグ回答は学習履歴へ入れない。
- カワムツの48×32px・2フレームアニメーションと、オイカワ・タカハヤ・鯉・錦鯉の48×32px静止画をゲーム・図鑑・未発見シルエットへ接続済み。

## 一部だけ完成しているもの

- 画面遷移：タイトル→初回のみ目標英検相当級→マップ→ポイント→問題設定→HIT→バトル→結果→次の魚まで動くが、UIは主にMaterial 3の仮デザイン。
- マップ：`麓の渓流`と`木漏れ日の浅瀬`のデータ、選択画面、仮のCompose Canvas背景はある。完成した3Dドット絵背景ではない。
- 魚：`Fish.kt`には15種あり、現在の渓流ではカワムツ、オイカワ、タカハヤ、鯉、錦鯉が出現する。他10種は海水魚の試作データ。
- 図鑑：保存と一覧表示は動くが、完成デザイン、詳細ページ、個体履歴、フィルター、報酬はない。
- リール：計算、A〜Zのリール本体、入力中の文字別・合計回転数、左から順に止まる正解演出、巻き上げバーとの同期は実装済み。完成デザイン、リール回転音、魚本体の巻き上げ表現はない。
- 学習：Room保存と復習ロジックはあるが、釣り堀、習得率による主・マップ解放はない。
- 文章問題：仕組みと1500問はあるが、現在はレベル1〜5の一部だけであり、全出題範囲へ拡充していない。

# 現在作業中の内容

- 語彙拡張は権利と品質を優先し、`tools/import_extended_vocabulary.py`でWortUniversum（CC BY-SA 4.0）、CEFR-J、Octanove C1/C2（CC BY-SA 4.0）、Open English WordNet 2024（CC BY 4.0）、Japanese WordNet 2.0（WordNet系ライセンス）、EJDict-hand（CC0）を照合した。英字見出し語、品詞、CEFR、同義概念の日本語語義、EJDict本文への語義出現、問題表示の非衝突を満たす2,934語を`OPEN_VOCAB_EXTENDED_V1`として追加し、合計6,700語へ拡張した。既存3,766語のIDと順位は変更していない。自動校正は通しているが、追加2,934問を人が一問ずつ読む最終校閲は今後も必要。
- 直近は最初の渓流に出る魚の正式画像を組み込んでいた。
- カワムツは元Aseprite/GIFから2フレームを抽出し、100ms・130msで再生済み。
- オイカワはユーザー提供の完成PNGを1フレームの静止画として接続済み。アニメーション差分はまだない。
- タカハヤはユーザー提供の48×32px透過PNGを1フレームの静止画として接続済み。元PNGは`art-source/fish/fish_takahaya.png`にも保管している。
- 鯉はユーザー提供の48×32px透過PNGを1フレームの静止画として接続済み。元PNGは`art-source/fish/fish_koi.png`にも保管している。
- 錦鯉はユーザー提供の48×32px透過PNGを1フレームの静止画として接続済み。元PNGは`art-source/fish/fish_nishikigoi.png`にも保管している。
- アジはユーザー提供の48×32px透過PNGを1フレームの静止画として接続済み。元PNGは`art-source/fish/fish_aji.png`に保管している。完成案はHP55〜75、距離6〜10m、サイズ18〜40cm、COMMON。海マップ未実装のため最初の渓流抽選には追加しない。
- イワシはユーザー提供の48×32px透過PNGを1フレームの静止画として接続済み。提供ファイル名はアジ表記だったが、ユーザーの依頼を優先してイワシへ使用し、元PNGは`art-source/fish/fish_iwashi.png`に保管している。完成案はHP32〜45、距離3〜5m、サイズ10〜24cm、COMMON。海マップ未実装のため最初の渓流抽選には追加しない。
- マグロはユーザー提供の48×32px透過PNGを1フレームの静止画として接続済み。元PNGは`art-source/fish/fish_maguro.png`に保管している。完成案はHP155〜215、距離18〜30m、サイズ90〜240cm、LEGENDARY。海マップ未実装のため最初の渓流抽選には追加しない。
- 魚バランステストでは通常はレア度別の想定問題数を検証するが、完成値を決めたイワシは2〜3問、アジは4〜5問、マグロは10〜14問を魚種別の目標として検証する。
- `木漏れ日の浅瀬`の主は錦鯉。現段階では主の解放条件が未決定のため低確率の通常抽選に含め、バトルHUD・捕獲結果・捕獲後の図鑑で「主」と明示する。
- ユーザー提供の48×32px透過PNGの完成釣り針を`drawable-nodpi/fishing_hook.png`へ追加し、HIT画面で浮きから伸びる糸の先へ`FilterQuality.None`で表示する。元PNGは`art-source/tackle/fishing_hook.png`に保管する。
- 問題形式選択後のHIT画面はマップ選択用の川辺景色ではなく、水面光・泡・川底を描いた水中専用背景にする。抽選済みの実魚画像が右から針へ近づき、つついた後に針と糸を引いて左下へ動く約2.1秒の自動食いつき演出を行う。完了後だけHIT表示とバトル開始ボタンを出す。失敗判定や魚・学習の数値変更は行わない。
- 正解時は各リールの回転数と最後の合計を強調し、魚の周囲にも背景・影なしの太い等幅数字として散開表示する。
- 魚を釣り上げた瞬間は、通常魚・大物ともユーザー提供の約1.56秒の「水面に浮かび上がる」MP3を`raw/fish_catch.mp3`から1回だけ再生する。元ファイルは`art-source/audio/fish_catch.mp3`へ保管する。画面再構成で同じ捕獲音を重複再生しない。以前の大物用`fish_catch_large.mp3`は保管しているが現在は再生に使用しない。
- 捕獲音または大物捕獲音の終了後、結果画面にいる間はユーザー提供の約2.18秒の`raw/fish_flopping_loop.mp3`をループ再生する。次の魚、問題設定、ほかの画面へ移動した時やComposable破棄時に停止・解放する。元ファイルは`art-source/audio/fish_flopping_loop.mp3`へ保管する。
- 問題形式決定後に水中のHIT前画面へ入った時は、自動の仕掛け投入音としてユーザー提供の約1.03秒のMP3を`raw/reel_cast.mp3`から1回再生する。元ファイルは`art-source/audio/reel_cast.mp3`へ保管する。
- 自動投入音の開始から350ms後、魚が接近し始める瞬間にユーザー提供の約1.03秒の`raw/lure_splash.mp3`を着水音として1回再生する。元ファイルは`art-source/audio/lure_splash.mp3`へ保管する。
- 2026-09-10に`testDebugUnitTest assembleDebug`が成功。Room計装テストは接続端末がなく未実行。
- 2026-09-11にコンテスト提出準備として`README.md`、GitHub Actions、`ASSET_RIGHTS.md`、`CONTEST_RELEASE_CHECKLIST.md`を追加した。正式なアプリ名・applicationId・コードライセンス・署名済みrelease APKは未決定。MP3は効果音ラボ素材で、公式FAQによりゲームの操作・演出音、コンテスト、GitHub公開へ使用可能と確認した。重複する`art-source/audio/`はGit管理対象外にする。

# 重要な仕様

- リール開始位置は必ずA。前の文字から次の文字までA〜Zを時計回りに進む。同じ文字へ移動するときも26進む。
- 正解送信後は全リールが同時に回り、最初のリールは625msで停止、その後は200ms間隔で左から順に停止する。各文字の正解演出後、全リール正解の全体フラッシュと「全部正解！」を700ms表示し、その完了まで次問へ進まない。
- 巻き上げバーは入力中に正解時の到達予定範囲を黄色で示す。正解送信後はリール演出の全時間に同期して、現在位置と距離表示が予定位置まで滑らかに進み、黄色の予定範囲を青色で埋める。
- Debugビルドの回答画面は「デバッグ操作」を閉じている時から縦スクロール可能にし、見切れた開閉ボタンまで移動できるようにする。操作欄を開いた時はそこまで自動スクロールする。デバッグボタンは狭い画面でも横にはみ出さない均等幅にする。Releaseビルドの通常画面にはこのスクロール条件を適用しない。
- `rotationSteps`は差が0なら26、それ以外はアルファベット上の正方向の差。`calculateRotation`は入力文字を順番に計算する。
- 魚へのダメージは`max(1, 合計回転数 / 5)`。
- 正解後のHPは`max(0, 現在HP - ダメージ)`、距離は`初期距離 × (新HP / 初期HP)`。距離0で捕獲する。
- 魚のHPは内部値であり、プレイヤーへ表示しない。距離は表示する。
- テンションは下がらない。異なる不正解で+15、スキップで+10。最初の10秒は時間加算なし、その後5秒ごとに+5。100で糸が切れて逃走する。
- 正解スコアは100点＋`max(0, 30 - 経過秒)`。
- 単語は一周中重複なし。周回後に再シャッフルし、境界で同じ単語を連続させない。
- 目標級の混合コースも、毎問の重み付きランダム抽選にはせず、級別の非重複デッキを20問の15・3・2構成へ並べる。一周中の単語重複と周回境界の同一単語連続を発生させない。
- 学習状態を動かすのは1問の最初の回答だけ。最初に失敗して後で正解した場合は補助あり完了として同じ履歴へ追記し、段階低下を重ねない。
- 問題の表示開始時点で対象語の保存済み回答回数が0なら、問題番号の横に「初出」を表示する。同じ問題の回答中は表示を維持し、回答済み・スキップ済みの語を次に出す際は表示しない。画面表示だけでは履歴へ書き込まない。
- 新しい問題の表示時は、問題カードを下からフェードインして軽く弾ませる開始演出を行う。色付きの縁取りは表示しない。同じ問題で不正解後に再回答する時は演出を繰り返さない。
- 正解数更新後のフィードバックで、次の問題カードを青い`primaryContainer`へ変色させない。新しい問題は通常の`surfaceContainer`背景で表示し、正解の視覚効果はリール側の緑演出で示す。
- 魚の捕獲結果では、その魚とのバトル中の初回正答率、一発正解・ミス後正解・スキップ件数、演出込みの釣り時間を表示する。「問題別の結果」には全問題の本文・最初の回答・正解・問題形式・初回回答時間・完了時間を表示する。この一時集計はRoom学習履歴とは分離し、次の魚でリセットする。
- 文章穴埋め入力の初回正解は自力想起、文章4択の初回正解は補助あり正解として保存する。4択だけでは正式復習段階を進めない。
- スキップと糸切れ時の未回答問題は失敗として記録する。画面離脱とデバッグ回答は学習成績へ入れない。
- 正式復習間隔は1日→3日→7日→習得後14日→30日。短期復習は別の4項目へ回答、または10分経過で解禁し、成功しても期限前に正式段階を進めない。
- 苦手単語は直近3回中2回以上失敗。英検級は公式単語表ではなくCEFR等に基づく「相当推定級」と明示する。
- 目標レベルは現在の実力判定ではなく、ユーザーが自分で選ぶ英検相当推定級とする。初回試し釣り、能力推定スコア、diagnostic専用問題、LearningPurpose、purposeTags、新difficulty 1〜5は実装しない。
- `words.json`変更時は、単語ID、順位、分類、問題文重複を壊さないよう`auditWords`を通す。
- 文章4択の誤答候補は正解語より難しいゲームレベルを使わない。接続詞など同レベルに3語ない場合は、より易しい既存語を使う。

# 重要なファイル

- `MainActivity.kt`：Composeアプリの入口。`GameViewModel`と`FishingGameApp`を起動する。
- `FishingGameApp.kt`：`GamePhase`に応じた全画面の切替と共通下部ナビゲーション。
- `GameViewModel.kt`：画面遷移、出題開始、入力、正誤、時間、テンション、捕獲・逃走、図鑑保存、学習履歴接続を統括する。
- `GameData.kt`：`Word`、分類enum、`GamePhase`、`GameUiState`、図鑑記録などの中心モデル。
- `GameLogic.kt`：リール、正解判定、時間・テンション、魚のHP・距離、問題送り、捕獲記録の純粋関数。
- `JsonLoader.kt`：`assets/words.json`を読み、`validateWordDataset`へ渡す。
- `app/src/main/assets/words.json`：6,700語の問題データ。直接編集時は監査必須。
- `Fish.kt`：魚・レア度・魚状態と15種の試作データ。
- `FishingArea.kt`：マップ、ポイント、出現魚ウェイト。現在は1マップ・1ポイント・5魚。
- `FishArtwork.kt`：魚別画像、フレームアニメーション、未発見シルエット。ピクセル画像は`FilterQuality.None`で表示する。
- `drawable-nodpi/fish_kawamutsu_0.png`、`fish_kawamutsu_1.png`、`fish_oikawa.png`、`fish_takahaya.png`、`fish_koi.png`、`fish_nishikigoi.png`、`fish_aji.png`、`fish_iwashi.png`、`fish_maguro.png`：表示用魚素材。
- `art-source/fish/`：カワムツのAseprite/GIFとオイカワ・タカハヤ・鯉・錦鯉・アジ・イワシ・マグロ元PNGの保管場所。
- `FishingGameScreen.kt`：英単語バトル、距離・テンション、問題、入力、デバッグ、結果UI。
- `AlphabetReel.kt`：A〜Zリール本体、文字別・合計回転数、入力中の回転アニメーション。
- `QuestionContent.kt`、`QuestionContentLoader.kt`：文章問題モデル、出題形式、検証、JSON読み込み、4択構築。
- `app/src/main/assets/question_content.json`：プロジェクト独自作成の文章問題1500問。
- `tools/audit_questions.py`：文章問題の参照・重複・空欄・選択肢・出典・権利情報をビルド前に監査する。
- `tools/review_questions.py`：文頭・句読点・空白・答え漏れ・冠詞・選択肢表示と品詞混在を全件確認する校正レポート。
- `tools/calibrate_question_choices.py`：既存語だけを使い、文中品詞と難易度を守って誤答候補を再構成する。通常は確認のみ、`--write`指定時だけJSONを更新する。
- `FishCollectionScreen.kt`、`FishCollectionStorage.kt`：図鑑表示とSharedPreferences保存。
- `TargetLevelSelectionScreen.kt`、`TargetLevelStorage.kt`：初回・変更時の目標英検相当級選択とSharedPreferences保存。
- `TargetCourseSelector.kt`：目標級・直下・基礎確認を75%・15%・10%で非重複に組み、学習履歴による下位語の優先順を決める純粋ロジック。
- `LearningModels.kt`、`LearningScheduler.kt`：学習状態、復習段階、回答結果、間隔反復アルゴリズム。
- `LearningDatabase.kt`、`LearningRepository.kt`：Roomの回答履歴・集約状態とトランザクション保存。
- `StreamDiorama.kt`：渓流の仮Canvas背景。完成背景ではない。
- `COMPLETION_CRITERIA.md`：編集可能な完成条件。
- `PROJECT_STATUS.md`、`WHAT_IS_NEEDED.md`：状態と大まかな残作業。一部に古い記述が残るため、実装確認はソースと本メモを優先する。
- `LEARNING_SYSTEM_IMPLEMENTATION.md`：学習履歴・復習ルールの実装記録。
- `app/src/test/`、`app/src/androidTest/`：ローカル単体テストとRoom計装テスト。

# これまでに決めたこと

- Android Studio＋Jetpack Composeで開発を続ける。Unityでマップを作って組み込む方式は採用していない。
- 完成背景は、背景・雲・木・水面などを透過画像レイヤーとしてCompose上で配置・アニメーションさせる方向。現在は配置用ビジュアルエディターを作っていない。
- 舞台は「里川」ではなく「渓流」、最初の仮マップは`麓の渓流`、最初のポイントは`木漏れ日の浅瀬`。
- 最初の出現魚はカワムツ、オイカワ、タカハヤ、鯉、錦鯉。カワムツの代用品Canvas魚は削除し、元のドット絵へ戻した。
- オイカワは存在しないフレームを補完せず、提供された1枚を静止画で使用する。
- タカハヤも存在しないフレームを補完せず、提供された1枚を静止画で使用する。
- 鯉も存在しないフレームを補完せず、提供された1枚を静止画で使用する。
- 錦鯉も存在しないフレームを補完せず、提供された1枚を静止画で使用する。
- 魚のHP表示案は採用しない。距離とテンションで状態を伝える。
- テンションを自然減少させる案は採用しない。時間、不正解、スキップで上がるだけにする。
- 長い辞書文をそのまま問題表示せず、代表意味＋品詞を出し、全文は補足として開く方式を採用した。
- 通常ユーザーの出題基準は目標の英検相当推定級へ一本化する。学年とゲームレベルは既存データ・検証・内部処理のため残すが通常UIでは選択させない。能力判定は行わない。
- 釣り堀で単語学習・復習することを最初の完成条件へ含めたが、実装はまだしていない。

# 未解決の問題

- 完成した3Dドット絵の渓流背景、動く雲、揺れる木・草、水流がない。
- マップ名・地域設定・ポイント数・最初の魚数・主の解放条件が正式決定していない。`木漏れ日の浅瀬`の主の魚種は錦鯉に決定済み。
- オイカワ、タカハヤ、鯉、錦鯉の泳ぐアニメーションがない。他の正式な渓流魚・画像も未追加。
- A〜Zリールの基本表示、正解時の文字別停止・全体正解演出、巻き上げバーとの同期はあるが、魚本体の巻き上げ表現、完成UI、リール回転音がない。
- 問題形式選択後は魚が針へ近づいて食いつく自動演出を正式仕様とし、手動の仕掛け投入・待機・アタリ・合わせ操作は追加しない。魚の抵抗、道具・餌・釣法は未実装。
- 釣り堀、主、ストーリー、マップ解放、学習率との連動がない。
- ボタン・HUD・タイトル・マップ・図鑑が標準Material中心で、完成デザインではない。
- 投入・着水・捕獲・捕獲後ループ音はあるが、BGM、環境音、リール音、正誤音、振動、チュートリアルがない。既存MP3は効果音ラボの利用規約とFAQに基づいて使用する。
- 文章問題1500問の構造・表記と選択肢品詞は全件校正済み。今後は実プレイで意味の紛らわしさを継続確認し、対象語を拡充する。
- Room計装テスト、Compose UIテスト、E2E、複数画面サイズ、実機確認が不足している。
- `PROJECT_STATUS.md`と`WHAT_IS_NEEDED.md`には、魚画像や学習保存を未実装とする古い箇所が一部残っている。
- 2026-09-10に魚・針・水中演出・リール表示・問題校正の変更をGitHub反映対象として整理した。`.idea`の端末ローカル設定は対象外。

# 次にやること

1. 追加語彙2,934語を人が一問ずつ最終校閲し、実プレイで誤義・不自然な日本語・多義語の分かりにくさを修正する。10,000語への次の拡張も、適法な出典と同じ照合・監査を通して段階的に行う。
2. 最初のマップ名・地域・ポイント・登場魚・主を正式決定し、最初の渓流背景を前景・中景・背景・雲・木・水面のレイヤーで制作・接続する。
3. 共通ボタンとHUDを3Dドット絵の完成デザインへ統一し、タイトル・マップ・ポイント・バトル・図鑑へ適用する。
4. 通常バトル画面のA〜Zリールを正解時の巻き上げ演出へ連携し、完成デザインと効果音を追加する。HIT前は現在の自動食いつき演出を調整して仕上げる。

# 注意事項

- 既存の問題表示、ASCII入力、リール計算、魚のHP・距離計算、テンション、問題の非重複、Room学習履歴を壊さない。
- `GameViewModel.kt`は多くの状態を同時更新する。画面遷移や回答処理を変える場合は、学習記録・タイマー・テンション・デバッグ除外も確認する。
- `Fish.kt`の全魚一覧と`FishingArea.kt`の出現候補は別。全魚から無条件抽選する旧関数を通常のポイント抽選へ戻さない。
- HPは表示しない。テンションを自動で下げない。同文字回転を0にしない。
- 魚画像は`drawable-nodpi`へ置き、ピクセル補間を有効にしない。カワムツの元アニメーション時間は100ms・130ms。
- 魚図鑑では捕獲済み・未発見とも薄いグレー単色（`#E7E9E8`）の標本背景と細いグレー枠（`#8A9698`）へ載せる。影や強いグラデーションは使わず、元画像の色やピクセル補間は変更しない。
- カワムツをCanvasで描き直した代用品は一度作ったが削除済み。必ず元ドット絵を使用する。
- `words.json`は第三者データの出典・ライセンスと分類根拠を持つ。英検分類を公式認定として表現しない。
- `git reset --hard`、`git clean`、一括checkoutをしない。大量の未コミット作業を消す危険がある。
- 変更後は`JAVA_HOME='/Applications/Android Studio.app/Contents/jbr/Contents/Home' ./gradlew testDebugUnitTest assembleDebug`を実行する。Gradleは`~/.gradle`へ書くため、環境によって権限許可が必要。

# 新しいチャットに最初に送る文章

このプロジェクトの開発を続けてください。最初に`/Users/moriryuhei/AndroidStudioProjects/fishinggame/HANDOFF_MEMO.md`を全文読み、そこに書かれた重要仕様・注意事項と現在の未コミット作業を守ってください。その後、関連する実ファイルを確認し、メモとソースに差があればソースを根拠に報告してください。既存の問題表示、入力、リール計算、魚のHP・距離、テンション、問題の非重複、学習履歴を壊さないでください。まず次の優先作業から、変更前に実装範囲を短く説明して進めてください。
